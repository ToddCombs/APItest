user_login_new:
  # 接口基本信息
  - request_info:
      url: /login/
      method: POST
      headers:
        Content-Type: application/json
    case_info:
        # 接口请求参数,params,json,data
      json:
        username: 17311111111
        password: 123456
      validate:
          # 通过json表达式拿到token，和后面的字符串做断言
        - contains: [$.token, 'asasaa']
        - contains: [$.token, 'asasaa']